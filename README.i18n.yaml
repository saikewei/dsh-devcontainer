# Bilingual-pair consistency record, following DeepSeek Harness's own convention
# (`@deepseek-ai/dsh-base/README.i18n.yaml`): the git blob hash of each side as of the last
# confirmed-consistent state. Both languages carry equal authority — neither is a
# translation of record that may lag the other.
#
# After editing EITHER side, bring the other along and re-record:
#
#   node test/readme-pair.mjs --write
#
# `npm run test:unit` fails when these hashes no longer match the files, so drift is caught
# by the suite rather than by a reader who happens to notice.
README.md: f88f0cc68f05eece4bd653bb5a00030f0c3b2204
README.zh.md: a7cf86da12f737bba4592092a1fe2db7ff89b843